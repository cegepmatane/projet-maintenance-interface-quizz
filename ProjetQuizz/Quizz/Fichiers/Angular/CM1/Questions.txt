Pourquoi Angular a vu jour en 2009 ? 
Qu'est-ce que TypeScript ? 
Le code TypeScript est transpiler en code Javascript pour ... 
var nomdelavariable:typede ........<type>=['variable'];
var nomdelavariable:typedu........[]=[variable]; 
Structure d'une variable : 
Que represente une valeur de type any ? 
Une interface vous permet de faconner vos objets et de faire en sorte que ... 
Un decorateur est un type special de declaration qui peut aªtre attache a diverses entites, telles que ...
Les decorateur fournissent aux developpeurs une maniere reutilisable ... 
Un decorateur utilise la forme : 
Au moment de l'execution, le decorateur sera execute avec le ...
L'ogjectif de Angular 2 est simple : reunir HTML et JavaScript d'une maniere ...
Les entites centrales sont : 
Chaque composant execute la liaison de donnees et la gestion des evenements avec son ... 
Les services sont crees pour toute tache, telle que ... 
Les services sont consommees par ... 
Les directives sont les instructions utilisees pour ...
Nous utilisaons le decorateur @NgModule pour creer le module de ...
Le ra´le d'un composant est de ...
Les composants sont generalement definis comme une classe ES2015 avec ...
Les classes sont exportee en tant que :
Qu'est-ce que c'est : import {Component} form '@angular/core' ; @Component({ ... }) ?
Les modeles (templates) sont utilises par ... 
Un modele est forme d'un melange de HTML basique combine avec ...
 import {Â Component } from ... 
Les plus grandes fonctionnalites d'Angular sont :
La maniere la plus simple de relier les donnees de votre classe de composants a votre modele ...
Une interpolation lie une valeur d'une propriete de classe avec votre template a l'aide de la syntaxe : 
template: '<h1>{{title}}</h1>' est le :
Qu'est ce qui lie une valeur de propriete d'un element HTML avec une valeur de propriete du ... 
 template = '<button [disabled]="isButtonDisabled"> My Button</button>' est une liaison :
Pour qu'un composant reponde a un evenement DOM genere a partir de la vue, Angular 2 vous ...
template = '<button (click)="showMessage()"> Show Message </button>', showMessage() est :
Pour ajouter une laison bidirectionnelle, il faut ajouter la propriete ngModel a ...
template = '<h1>Hello {{name}} </h1> <br><input [(ngModel)]="name">' est : 
Dans une liaison bidirectionnelle, l'entree lie le nom de propriete dans les deux sens, de sorte que chaque :
La propriete ngModel est appelee :